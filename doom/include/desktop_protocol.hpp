#pragma once

// Compatibility include for Doom's standalone build. The protocol ABI is
// defined once in userspace-support and packaged by neutrino-ui.
#include "../../userspace-support/desktop_protocol.hpp"
